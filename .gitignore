# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Local tooling / editor cruft
.claude/
.vscode/
.idea/
.DS_Store

# Logs & secrets
*.log
.env
.env.*

# Benchmark resume state points at absolute run dirs, so it is machine-specific.
# The reports and experiment.json are committed; these are not.
bench/*/state.json
bench/probe-out/

# Model cache lives in ~/.local-language-machine (outside the repo)
