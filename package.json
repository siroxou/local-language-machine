{
  "name": "local-language-machine",
  "version": "0.0.1",
  "private": true,
  "description": "All-in-one, fully offline local AI coding — self-contained inference, zero external services.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "tsx --test \"src/**/*.test.ts\"",
    "bench:data": "tsx bench/gen-dataset.ts",
    "bench": "tsx bench/run-matrix.ts",
    "preview": "tsx src/preview/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "node-llama-cpp": "^3.19.1",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/ws": "^8.18.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
